{
  "name": "iotdbfrontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "js-cookie": "^2.2.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.2"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.11.9",
    "@testing-library/react": "^11.2.5",
    "@testing-library/user-event": "^12.7.1"
  },
  "scripts": {
    "start": "HTTPS=true SSL_CRT_FILE=./../server-cert.pem SSL_KEY_FILE=./../server-key.pem react-scripts start",
    "build": "HTTPS=true SSL_CRT_FILE=./../server-cert.pem SSL_KEY_FILE=./../server-key.pem react-scripts build",
    "test": "react-scripts test"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
